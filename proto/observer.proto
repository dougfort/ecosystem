syntax = "proto3";

package observer;

service EventObserver {
    rpc Events(Join) returns (stream Event) {}
}

message Join {
    string event_source = 1;
}

message Event {
    int32 id = 1;
    int32 type = 2;
    int32 food_kind = 3;
    int32 food_amount = 4;
}
